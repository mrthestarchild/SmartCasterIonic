import { PadMinimumDigitsPipe } from './pad-minimum-digits.pipe';

describe('PadMinimumDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new PadMinimumDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});

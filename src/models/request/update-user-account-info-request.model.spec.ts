import { UpdateUserAccountInfoRequest } from "./update-user-account-info-request.model";

describe('UpdateUserAccountInfoRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateUserAccountInfoRequest()).toBeTruthy();
  });
});

import { PadCollectionRequest } from './pad-collection-request.model';

describe('PadCollectionRequest', () => {
  it('should create an instance', () => {
    expect(new PadCollectionRequest()).toBeTruthy();
  });
});

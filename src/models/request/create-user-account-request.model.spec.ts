import { CreateUserAccountRequest } from "./create-user-account-request.model";

describe('CreateUserAccountRequest', () => {
  it('should create an instance', () => {
    expect(new CreateUserAccountRequest()).toBeTruthy();
  });
});

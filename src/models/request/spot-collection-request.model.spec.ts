import { SpotCollectionRequest } from './spot-collection-request.model';

describe('SpotCollectionRequest', () => {
  it('should create an instance', () => {
    expect(new SpotCollectionRequest()).toBeTruthy();
  });
});

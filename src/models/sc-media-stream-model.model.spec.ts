import { ScMediaStreamModel } from './sc-media-stream-model.model';

describe('ScMediaStreamModel', () => {
  it('should create an instance', () => {
    expect(new ScMediaStreamModel()).toBeTruthy();
  });
});

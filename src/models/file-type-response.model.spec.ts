import { FileTypeResponse } from './file-type-response.model';

describe('FileTypeResponse', () => {
  it('should create an instance', () => {
    expect(new FileTypeResponse()).toBeTruthy();
  });
});

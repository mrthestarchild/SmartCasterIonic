import { DropContainerBoundingBox } from './drop-container-bounding-box.model';

describe('DropContainerBoundingBox', () => {
  it('should create an instance', () => {
    expect(new DropContainerBoundingBox()).toBeTruthy();
  });
});

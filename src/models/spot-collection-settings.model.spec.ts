import { SpotCollectionSettings } from "./spot-collection-settings.model";

describe('SpotCollectionSettings', () => {
  it('should create an instance', () => {
    expect(new SpotCollectionSettings()).toBeTruthy();
  });
});

import { SoundLevelCollectionSetting } from './sound-level-collection-setting.model';

describe('SoundLevelCollectionSetting', () => {
  it('should create an instance', () => {
    expect(new SoundLevelCollectionSetting()).toBeTruthy();
  });
});

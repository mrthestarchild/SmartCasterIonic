import { LocalServiceResponse } from './local-service-response.model';

describe('LocalServiceResponse', () => {
  it('should create an instance', () => {
    expect(new LocalServiceResponse()).toBeTruthy();
  });
});

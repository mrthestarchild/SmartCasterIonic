import { SoundLevelCollectionResponse } from './sound-level-collection-response.model';

describe('SoundLevelCollectionResponse', () => {
  it('should create an instance', () => {
    expect(new SoundLevelCollectionResponse()).toBeTruthy();
  });
});

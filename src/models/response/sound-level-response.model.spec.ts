import { SoundLevelResponse } from "./sound-level-response.model";

describe('SoundLevelResponse', () => {
  it('should create an instance', () => {
    expect(new SoundLevelResponse()).toBeTruthy();
  });
});

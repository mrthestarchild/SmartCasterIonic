import { PadCollectionResponse } from "./pad-collection-response.model";

describe('PadCollectionResponse', () => {
  it('should create an instance', () => {
    expect(new PadCollectionResponse()).toBeTruthy();
  });
});

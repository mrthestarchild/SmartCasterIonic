import { CreateUserAccountResponse } from "./create-user-account-response.model";

describe('CreateUserAccountResponse', () => {
  it('should create an instance', () => {
    expect(new CreateUserAccountResponse()).toBeTruthy();
  });
});

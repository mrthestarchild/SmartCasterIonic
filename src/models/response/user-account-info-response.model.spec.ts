import { UserAccountInfoResponse } from "./user-account-info-response.model";

describe('UserAccountInfoResponse', () => {
  it('should create an instance', () => {
    expect(new UserAccountInfoResponse()).toBeTruthy();
  });
});

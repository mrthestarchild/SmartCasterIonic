import { StyleHeightWidthExtension } from './style-height-width-extension.model';

describe('StyleHeightWidthExtension', () => {
  it('should create an instance', () => {
    expect(new StyleHeightWidthExtension()).toBeTruthy();
  });
});

import { PadCollectionSettings } from "./pad-collection-settings.model";

describe('PadCollectionSettings', () => {
  it('should create an instance', () => {
    expect(new PadCollectionSettings()).toBeTruthy();
  });
});
